基于我对项目的全面分析，以下是前端页面的缺失功能：

---

## 前端页面缺失功能分析

### 1. BooksPage（书架页）

| 缺失功能 | 严重程度 | 说明 |
|----------|---------|------|
| ❌ 错误状态处理 | 🔴 高 | `useQuery` 的 `error` 未使用，网络异常时无任何反馈 |
| ❌ 分类筛选下拉 | 🟡 中 | API 支持 `category` 参数，但前端没有筛选项 |
| ❌ 搜索/排序 | 🟡 中 | 无法按书名搜索、按日期/笔记数排序 |
| ❌ 分页 | 🟡 中 | 写死 `page_size=100`，超过 100 本时无法加载更多 |
| ❌ 刷新按钮 | 🟢 低 | 无手动刷新入口 |

### 2. BookDetailPage（书籍详情页）

| 缺失功能 | 严重程度 | 说明 |
|----------|---------|------|
| ❌ 错误状态处理 | 🔴 高 | 查询失败时无反馈 |
| ❌ 编辑书籍信息 | 🟡 中 | 无法修改书名、作者、分类等，仅能删除 |
| ❌ AI 分析入口 | 🟡 中 | 详情页没有触发 AI 分析的按钮，需要跑到图谱页 |
| ❌ 高亮分页/筛选 | 🟡 中 | 写死 `page_size=500`，没有按章节筛选 |
| ❌ 删除单条高亮 | 🟢 低 | 只能删整本书，不能删单独笔记 |
| ❌ 高亮搜索 | 🟢 低 | 不能在当前书籍内搜索笔记内容 |
| ❌ 导出功能 | 🟢 低 | 无法导出该书笔记 |

### 3. ImportPage（导入页）

| 缺失功能 | 严重程度 | 说明 |
|----------|---------|------|
| ❌ 错误详情展示 | 🔴 高 | 仅 `message.error`，解析失败不显示具体原因 |
| ❌ 格式校验前置 | 🟡 中 | 没有前端预检文件格式，上传后才发现错误 |
| ❌ 预览功能 | 🟡 中 | 没有导入前展示解析结果（书名、作者、笔记数预览） |
| ❌ 批量导入 | 🟢 低 | 一次只能上传一个文件 |
| ❌ 进度展示 | 🟢 低 | 只有文字提示，没有进度条 |

### 4. SearchPage（搜索页）

| 缺失功能 | 严重程度 | 说明 |
|----------|---------|------|
| ❌ 错误状态处理 | 🔴 高 | 查询失败无反馈 |
| ❌ 高级筛选 | 🟡 中 | 无法按书籍范围、分类、日期筛选 |
| ❌ 搜索历史 | 🟢 低 | 没有记录搜索关键词 |
| ❌ 匹配高亮 | 🟢 低 | 搜索结果中不标记匹配词 |

### 5. ProfilePage（阅读画像页）

| 缺失功能 | 严重程度 | 说明 |
|----------|---------|------|
| ❌ 错误状态处理 | 🔴 高 | 无错误处理 |
| ❌ **时间线视图** | 🔴 高 | 后端有 `/api/v1/timeline` 接口，但**前端完全缺失时间线页面**！ |
| ❌ 趋势图表 | 🟡 中 | 只有数字和进度条，缺少折线图/柱状图（阅读趋势） |
| ❌ 概念词云 | 🟡 中 | 没有从 Profile API 获取概念分布并可视化 |
| ❌ 可点击分类 | 🟢 低 | 分类列表不可点击，无法跳转筛选 |

### 6. GraphPage（知识图谱页）

| 缺失功能 | 严重程度 | 说明 |
|----------|---------|------|
| ❌ **真正的图可视化** | 🔴 高 | 目前只是 Tag + 文字列表，没有引入任何图可视化库（G6/d3/vis-network）实现力导向图 |
| ❌ 节点交互 | 🔴 高 | 节点不可点击、不可拖拽、不可展开 |
| ❌ 错误状态处理 | 🔴 高 | useQuery 错误处理不完整 |
| ❌ 图布局切换 | 🟡 中 | 没有力导向/树形/圆形等布局切换 |
| ❌ 子图钻取 | 🟡 中 | 无法点击节点展开子图 |
| ❌ 导出图片 | 🟢 低 | 无导出 PNG/SVG 功能 |

### 7. 全局缺失

| 缺失功能 | 严重程度 | 说明 |
|----------|---------|------|
| ❌ **时间线页面** | 🔴 高 | 后端 `/api/v1/timeline` 已实现，前端无对应页面和路由 |
| ❌ **404 页面** | 🟡 中 | 未匹配路由没有 fallback |
| ❌ **全局错误边界** | 🟡 中 | 未捕获的 React 错误会导致白屏 |
| ❌ `store/` 和 `hooks/` | 🟢 低 | 目录为空，Zustand 已安装但未使用 |
| ❌ 响应式优化 | 🟢 低 | 移动端侧边栏无自动折叠 / Drawer 模式 |
| ❌ 深色模式 | 🟢 低 | 无主题切换 |

---

### 优先级建议

1. **P0 - 必须补齐**：时间线页面、图可视化、全局错误处理
2. **P1 - 强烈建议**：分类筛选、编辑功能、AI 分析入口整合
3. **P2 - 锦上添花**：批量操作、搜索历史、导出、深色模式